// globalRouter
const HOME = "/";
const JOIN = "/join";
const PHOTOS = "/photos";
const PROFILE = "/profile";

const routes = {
  home: HOME,
  join: JOIN,
  photos: PHOTOS,
  profile: PROFILE
};

export default routes;
